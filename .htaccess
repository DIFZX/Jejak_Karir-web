Options -Indexes

<IfModule mod_authz_core.c>
    <FilesMatch "(^\.|\.sql$|\.log$|\.ini$)">
        Require all denied
    </FilesMatch>
</IfModule>

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(?:backups|scripts)(?:/|$) - [F,L,NC]
</IfModule>
